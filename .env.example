# Backend origin the Next.js server proxies to under /api/*
# Leave empty to call the API directly (requires CORS + non-cross-site cookies)
API_PROXY_TARGET=http://localhost:3000

# Base URL used by the browser. Use "/api" to go through the proxy above.
NEXT_PUBLIC_API_URL=/api
